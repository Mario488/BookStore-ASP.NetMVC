@model BookeStore_application.Models.Domain.Publisher;
@{
}

<h1>Add Publisher</h1>
<div class="container pt-5">
    <div class="col-md-6">
        <form asp-action="Add" method="post">
            <div class="form-group">
                <label asp-for="PublisherName">PublisherName *</label>
                <input type="text" class="form-control" asp-for="PublisherName" />
                <span asp-validation-for=PublisherName class="text-danger"></span>
            </div>
            @if(TempData["msg"] != null){
                <div class="form-group">
                    <span class="danger">@TempData["msg"]</span>
                </div>
            }
            <div class="form-group">
            <button type="submit" class="btn btn-dark">Save</button>
            <a href="/Publisher/GetAll" class="btn btn-primary">List</a>
            </div>
        </form>
    </div>
</div>
